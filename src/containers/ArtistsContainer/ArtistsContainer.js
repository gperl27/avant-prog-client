import React from 'react';
import PropTypes from 'prop-types';

const ArtistsContainer = props =>
  (
    <div>
      ArtistsContainer
    </div>
  );

ArtistsContainer.propTypes = {
  ArtistsContainerProps: PropTypes.obj
};

export default ArtistsContainer;
